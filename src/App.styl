.header
  background-color: #000
  color:#fff
  height: 50px
  width 100vw
  display flex
  flex-direction: row
  justify-content: space-between
  align-items: center
  padding 0 1rem

.brand
  cursor: pointer
  font-size 1em
  color #fff

.content
  padding 1em
